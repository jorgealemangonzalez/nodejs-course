extends layouts/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/user.css")

block content
    main
        if users.length > 0
            ul
                each user in users
                    li #{user.name}
        else
            h1 No Users